/**
 * The class <code>Database</code> is an abstraction layer for a simple
 * database which works on top of the OS filesystem.
 */
public class Database {
    /**
     * The default constructor creates a new <code>Database</code> instance with
     * the provided path. It creates a folder and takes it as a reference for
     * storing future files.
     *
     * @param path The path to the target folder where all the information will be
     *             kept.
     */
    public Database(String path) {

    }
}
