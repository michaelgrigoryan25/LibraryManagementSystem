package user.admin;

import user.User;

public class Admin extends User {

    public Admin(Long id, String firstName, String secondName, String username) {
        super(id, firstName, secondName, username);
    }
}
